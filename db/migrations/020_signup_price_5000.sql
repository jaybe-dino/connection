-- 확정 요구사항: 브랜드별 "신규" 검증 크리에이터 가입 1명당 5,000원 (월합산).
-- 이 파일은 정책 단가만 바꾼다 — 기존 사용량 기록은 건드리지 않는다.
--
-- [이력 정정 기록] 이 파일의 최초 버전(8172db3 이전, 835eb76)은 미청구
-- 사용량 전체를 5,000원으로 일괄 UPDATE 했다. 이는 소급 인상 금지 원칙 위반으로
-- 검수에서 지적되어 제거했다. 최초 버전이 이미 실행된 환경의 영향 행은
-- 024_signup_usage_audit.sql 이 감사 대상으로 수집하며, 자동 재변경은 하지 않는다.
-- (마이그레이션 러너는 파일명 기준 추적이므로 본 수정은 재실행되지 않는다.)
UPDATE signup_billing_policy SET unit_price = 5000 WHERE unit_price <> 5000;
